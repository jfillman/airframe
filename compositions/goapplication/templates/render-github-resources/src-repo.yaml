# Src repo + Go boilerplate. Rendered by function-go-templating (source: Inline, see
# ../composition.yaml/../build-composition.sh), using double-angle-bracket
# delimiters - same convention as nodejsapplication's own src-repo.yaml.
#
# repo.github.m.upbound.io (namespaced managed resources), not the Cluster-scoped
# repo.github.upbound.io family. providerConfigRef.kind: ClusterProviderConfig is
# required, not defaulted.
#
# crossplane.io/external-name on the Repository below - same live-bug fix as
# nodejsapplication's own src-repo.yaml.
#
# Every external-name value below is double-quoted (fixed 2026-09-04) - the unquoted
# "<repo>:<file>:" scalar's trailing bare colon is invalid plain-YAML. Real, fleet-wide
# live bug (every app on kiac-dev was Synced: False from this) - see
# springbootapplication's own copy of this file for the full writeup.
#
# managementPolicies: ["Create", "Observe", "LateInitialize", "Delete"] (no "Update")
# on every RepositoryFile below - same reasoning as nodejsapplication's own scaffold
# files: real app source a developer is expected to edit immediately after
# scaffolding.
#
# Zero external dependencies in the scaffold itself (net/http stdlib, no framework) -
# same minimalism NodeJSApplication's own boilerplate keeps.
#
# $modulePath: github.com/<platformOwner>/<xrName>, matching the real repo URL - see
# xrds/goapplication.yaml's own header for why this is a computed literal, not a
# spec field.
#
# crossplane.io/external-name on every RepositoryFile below, format "<repo>:<file>:" -
# same fix as nodejsapplication's own src-repo.yaml: a cluster rebuild wipes Crossplane's
# state but not GitHub's, so a stateless RepositoryFile MR defaults to Create, and
# overwriteOnCreate: true means Create silently overwrites real committed developer
# edits. Fixed here preemptively, same as pythonapplication's own src-repo.yaml.
<<- $xrName := .observed.composite.resource.metadata.name >>
<<- $spec := .observed.composite.resource.spec >>
<<- $description := $spec.description | default (printf "%s - a Go application" $xrName) >>
<<- $goVersion := $spec.goVersion >>
<<- $port := $spec.port >>
<<- $visibility := $spec.visibility >>
<<- $platformOwner := "jfillman" >>
<<- $modulePath := printf "github.com/%s/%s" $platformOwner $xrName >>
# ---------------------------------------------------------------------------
# Scaffold files never delete or overwrite developer content, and never re-identify.
# (Added 2026-09-23 after a real incident - read before changing anything here.)
#
# These RepositoryFile resources exist only to seed a brand-new app's repo. After the
# first commit the file belongs to the developer. They used to be reconciled forever
# with policies Create, Observe, LateInitialize, Delete and overwriteOnCreate true, which
# had two real failure modes:
#   1. A composition change to a field of an ALREADY-COMPOSED resource is a fleet-wide
#      write even without an Update policy. The 2026-09-22 Dockerfile-to-Containerfile
#      rename rewrote file: and external-name on every existing resource while the
#      provider's Terraform state (and the repo) still said Dockerfile. file: is a
#      force-replace field, so the provider could neither update nor settle, and
#      crossplane-runtime retried at its 60s backoff cap ignoring --poll: 7 resources,
#      roughly 16,000 GitHub calls/hour against a 5,000/hour budget shared with Backstage.
#   2. Delete let a mis-observed 403 or a resource-name change remove a developer-owned
#      file, and overwriteOnCreate true let a mis-observed 403 overwrite real content.
#
# Therefore: no Delete policy (deleting the Kubernetes object can never remove a file
# from GitHub), overwriteOnCreate false, and the identity fields (file:, external-name)
# of an EXISTING resource are pinned to what the provider itself reports it manages
# (status.atProvider.file), falling back to the live spec, then to the template default.
# The default therefore only ever applies to a resource that does not exist yet, so a
# future change of the default can never re-identify a live object. A cluster rebuild
# still adopts existing files: nothing is observed, external-name is set, Observe adopts.
# Do NOT try to make these objects disappear after creation by rendering nothing: a
# missing object looks identical to a brand-new one, so the next reconcile recreates it
# (proved live on 2026-09-23 - an endless create/garbage-collect loop, ~800 GitHub calls
# in 4 minutes). If retirement is ever wanted it needs a durable marker outside this
# render, and a multi-pass test.
# NOTE for editors: never write two adjacent angle-bracket characters in comments (see
# the note further down) - this block deliberately avoids them.
# ---------------------------------------------------------------------------
<<- define "scaffoldFile" ->>
<<- $f := "" ->>
<<- if .obs ->>
<<-   $f = dig "resource" "status" "atProvider" "file" "" .obs ->>
<<-   if not $f ->><<- $f = dig "resource" "spec" "forProvider" "file" "" .obs ->><<- end ->>
<<- end ->>
<<- if $f ->><<- $f ->><<- else ->><<- .default ->><<- end ->>
<<- end >>
apiVersion: repo.github.m.upbound.io/v1alpha1
kind: Repository
metadata:
  name: <<$xrName>>-src
  annotations:
    <<setResourceNameAnnotation "src-repo">>
    crossplane.io/external-name: <<$xrName>>
spec:
  providerConfigRef:
    kind: ClusterProviderConfig
    name: default
  forProvider:
    name: <<$xrName>>
    description: <<$description | toJson>>
    visibility: <<$visibility>>
    autoInit: true
    hasIssues: true
    deleteBranchOnMerge: true
    securityAndAnalysis:
      - secretScanning:
          - status: disabled
        secretScanningPushProtection:
          - status: disabled
---
apiVersion: repo.github.m.upbound.io/v1alpha1
kind: RepositoryFile
metadata:
  name: <<$xrName>>-src-go-mod
  annotations:
    <<setResourceNameAnnotation "src-repo-go-mod">>
    crossplane.io/external-name: "<<printf "%s:go.mod:" $xrName>>"
spec:
  managementPolicies: ["Create", "Observe", "LateInitialize"]
  providerConfigRef:
    kind: ClusterProviderConfig
    name: default
  forProvider:
    repository: <<$xrName>>
    file: go.mod
    overwriteOnCreate: false
    content: |
      module <<$modulePath>>

      go <<$goVersion>>
    commitMessage: "GoApplication scaffold: go.mod"
---
apiVersion: repo.github.m.upbound.io/v1alpha1
kind: RepositoryFile
metadata:
  name: <<$xrName>>-src-main-go
  annotations:
    <<setResourceNameAnnotation "src-repo-main-go">>
    crossplane.io/external-name: "<<printf "%s:main.go:" $xrName>>"
spec:
  managementPolicies: ["Create", "Observe", "LateInitialize"]
  providerConfigRef:
    kind: ClusterProviderConfig
    name: default
  forProvider:
    repository: <<$xrName>>
    file: main.go
    overwriteOnCreate: false
    content: |
      package main

      import (
      	"encoding/json"
      	"fmt"
      	"log"
      	"net/http"
      	"os"
      )

      func main() {
      	port := os.Getenv("PORT")
      	if port == "" {
      		port = <<printf "%d" $port | toJson>>
      	}

      	http.HandleFunc("/healthz", func(w http.ResponseWriter, r *http.Request) {
      		w.Header().Set("Content-Type", "application/json")
      		json.NewEncoder(w).Encode(map[string]string{"status": "ok"})
      	})
      	http.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) {
      		fmt.Fprintf(w, <<printf "Hello from %s\n" $xrName | toJson>>)
      	})

      	log.Printf(<<printf "%s listening on port %%s" $xrName | toJson>>, port)
      	log.Fatal(http.ListenAndServe(":"+port, nil))
      }
    commitMessage: "GoApplication scaffold: main.go"
---
<<- $obsD := index (.observed.resources | default dict) "src-repo-dockerfile" >>
<<- $pinnedFile := include "scaffoldFile" (dict "obs" $obsD "default" "Containerfile") >>
apiVersion: repo.github.m.upbound.io/v1alpha1
kind: RepositoryFile
metadata:
  # metadata.name stays -src-dockerfile (not -src-containerfile): it is Crossplane's
  # correlation key for an EXISTING XR's already-composed resource. file: and the
  # external-name are pinned to the provider's own state for an existing resource
  # (scaffoldFile at the top of this file); the default below only applies to a
  # resource that does not exist yet. See the header comment for the incident.
  name: <<$xrName>>-src-dockerfile
  annotations:
    <<setResourceNameAnnotation "src-repo-dockerfile">>
    crossplane.io/external-name: "<<printf "%s:%s:" $xrName $pinnedFile>>"
  # Multi-stage, matching springbootapplication's own precedent (a real difference
  # from nodejsapplication's single-stage script, not just a style choice): build
  # stage carries the full Go toolchain, final stage is bare Alpine running only the
  # compiled static binary - keeps the shipped image's dependency surface to just
  # the binary plus Alpine's own base packages. CGO_ENABLED=0 - the resulting binary
  # has no libc dependency, so it runs unmodified on the runtime stage's musl libc.
  # `apk upgrade` on the runtime stage picks up already-published Alpine package
  # patches, same as nodejsapplication's own Containerfile.
spec:
  # No "Update" and no "Delete": this object must never write to or remove a
  # developer-owned file (see the header comment).
  managementPolicies: ["Create", "Observe", "LateInitialize"]
  providerConfigRef:
    kind: ClusterProviderConfig
    name: default
  forProvider:
    repository: <<$xrName>>
    file: <<$pinnedFile>>
    overwriteOnCreate: false
    content: |
      FROM golang:<<$goVersion>>-alpine AS build
      WORKDIR /app
      COPY go.mod ./
      COPY main.go ./
      RUN CGO_ENABLED=0 go build -o app .

      FROM alpine:latest
      WORKDIR /app
      RUN apk upgrade --no-cache
      COPY --from=build /app/app .
      EXPOSE <<$port>>
      CMD ["./app"]
    commitMessage: "GoApplication scaffold: Containerfile"
---
apiVersion: repo.github.m.upbound.io/v1alpha1
kind: RepositoryFile
metadata:
  name: <<$xrName>>-src-gitignore
  annotations:
    <<setResourceNameAnnotation "src-repo-gitignore">>
    crossplane.io/external-name: "<<printf "%s:.gitignore:" $xrName>>"
spec:
  managementPolicies: ["Create", "Observe", "LateInitialize"]
  providerConfigRef:
    kind: ClusterProviderConfig
    name: default
  forProvider:
    repository: <<$xrName>>
    file: .gitignore
    overwriteOnCreate: false
    content: |
      /app
      *.log
      .env
    commitMessage: "GoApplication scaffold: .gitignore"
---
apiVersion: repo.github.m.upbound.io/v1alpha1
kind: RepositoryFile
metadata:
  name: <<$xrName>>-src-readme
  annotations:
    <<setResourceNameAnnotation "src-repo-readme">>
    crossplane.io/external-name: "<<printf "%s:README.md:" $xrName>>"
spec:
  managementPolicies: ["Create", "Observe", "LateInitialize"]
  providerConfigRef:
    kind: ClusterProviderConfig
    name: default
  forProvider:
    repository: <<$xrName>>
    file: README.md
    overwriteOnCreate: false
    content: |
      # <<$xrName>>

      <<$description>>

      Scaffolded by `GoApplication` (Airframe). Go <<$goVersion>>.

      ```
      go run .
      ```

      **CICD pipeline onboarding**: this app's `GoApplication` resource carries a
      `CicdOnboarded` condition tracking whether `platform-cicd`'s tenant-onboarding
      ApplicationSet has picked up this app's committed tenant identity yet. See the
      resource's own status for details.
    commitMessage: "GoApplication scaffold: README.md"
